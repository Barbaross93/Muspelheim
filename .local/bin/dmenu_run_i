#!/bin/sh
# dmenu_run improved
# if a command ends with "!", it is started in term.

termcmd="$HOME/.local/bin/sterm"

cmd="$(dmenu_path | ddmenu $@)"
case $cmd in
*\!) ${termcmd} -e "$(printf "%s" "${cmd}" | cut -d'!' -f1)" ;;
*) ${cmd} ;;
esac
exit
