#!/bin/sh
# dmenu_run improved
# if a command ends with "!", it is started in term.

termcmd="sterm -e"

cmd="$(dmenu_path | ddmenu $@)"
case $cmd in
*\!) ${termcmd} "$(printf "%s" "${cmd}" | cut -d'!' -f1)" ;;
*) ${cmd} ;;
esac
exit
